import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  controlsContainer: {
    padding: 30,
    marginVertical: 30,
  },
  container: {
    padding: 30,
  },
  buttonContainer: {
    marginVertical: 30,
  },
});

export default styles;
